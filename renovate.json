{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "packageRules": [
    {
      "description": "Batch all @joint/* runtime packages into ONE PR. groupName only — NOT registered as a monorepo, so members are allowed to sit at independent versions (react 4.3.1 while core 4.3.0) without the PR ever going 'immortal'. rangeStrategy scoped HERE so it only affects @joint/*, not every dependency in the project.",
      "matchPackageNames": ["/^@joint//"],
      "groupName": "joint",
      "rangeStrategy": "replace"
    }
  ]
}
